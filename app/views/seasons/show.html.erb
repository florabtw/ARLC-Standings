<p id="notice"><%= notice %></p>

<div class="text-right">
  <%= link_to 'Edit Season', edit_season_path(@season), class: 'btn btn-default' %>
</div>

<h1 class="text-center">
  <%= @season.name %>
  <small>
    <%= @season.division %>
  </small>
</h1>

<div class="row">
  <div class="col-sm-6">
    <h3>Teams</h3>
  </div>
  <div class="col-sm-6 text-right">
    <%= link_to 'New Team', new_season_team_path(@season), class: 'btn btn-success' %>
  </div>
</div>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @season.teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td><%= link_to 'Show', team, class: 'btn btn-info' %></td>
        <td><%= link_to 'Edit', edit_team_path(team), class: 'btn btn-primary' %></td>
        <td>
          <%= link_to 'Delete', team,
                  {
                    method: :delete,
                    data: { confirm: 'Are you sure?' },
                    class: 'btn btn-danger'
                  }
          %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<div class="row">
  <div class="col-sm-6">
    <h3>Matches</h3>
  </div>
  <div class="col-sm-6 text-right">
      <%= link_to 'New Match', new_season_match_path(@season), class: 'btn btn-success' %>
  </div>
</div>
<table class="table">
  <thead>
    <tr>
      <th>Home team</th>
      <th>Away team</th>
      <th>Match status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @season.matches.each do |match| %>
      <tr>
        <td><%= match.home_team %></td>
        <td><%= match.away_team %></td>
        <td><%= match.match_status %></td>
        <td><%= link_to 'Show', match, class: 'btn btn-info' %></td>
        <td><%= link_to 'Edit', edit_match_path(match), class: 'btn btn-primary' %></td>
        <td>
          <%= link_to 'Delete', match,
                  {
                    method: :delete,
                    data: { confirm: 'Are you sure?' },
                    class: 'btn btn-danger'
                  }
          %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
